{
  "name": "asseteroids",
  "version": "0.0.0",
  "description": "Asteroids with your assets.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "beef": "beefy --live --debug=true index.js",
    "start": "node server.js",
    "watch": "watchify -d -o static/bundle.js -e index.js",
    "build": "browserify -d -o static/bundle.js -e index.js"
  },
  "author": "Mark Feltner",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^3.44.2",
    "bower": "^1.3.3",
    "watchify": "^0.8.2",
    "beefy": "^1.1.0"
  },
  "dependencies": {
    "raphael": "^2.1.0",
    "jquery": "^2.1.1"
  }
}
